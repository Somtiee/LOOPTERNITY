# Vercel (production app)
# Copy these into the Vercel project (Production + Preview). Never add PRIVATE_KEY.

NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Optional READ FALLBACK RPC. Robinhood Chain mainnet (4663) is RETIRED —
# remove any Robinhood value. Primary is always the chain's public RPC; this
# URL is only tried when the public RPC fails. E.g. a chain-restricted
# Alchemy HTTPS URL. NEXT_PUBLIC_ values are visible in the browser — use a
# scoped/restricted key, never an admin key. Leave empty for public-only.
# NEXT_PUBLIC_RPC_URL=

# LOOPITERNS ERC-721. The Robinhood (4663) deployment is RETIRED — this will
# hold the Circle Arc testnet (5042002) contract address after the pivot.
# Empty / zero = mint disabled.
# OPERATOR NOTE: on Vercel, remove the Robinhood value of
# NEXT_PUBLIC_LOOPITERNS_ADDRESS (and the Robinhood NEXT_PUBLIC_RPC_URL if
# set) and redeploy — the LOOPITERNS mint + inventory UI hides itself; art
# files stay.
NEXT_PUBLIC_LOOPITERNS_ADDRESS=
# Optional fallback if mintPrice() cannot be read. Wei only, no USD.
# NEXT_PUBLIC_LOOPITERNS_MINT_PRICE_WEI=

# Server-only (Vercel: NOT exposed to the client). Private key that signs
# mint vouchers for /api/loopitern/voucher. Its public address must equal
# MINT_SIGNER_ADDRESS in contracts/.env at deploy time.
# Generate: cast wallet new  (never commit the key)
VOUCHER_SIGNER_PRIVATE_KEY=

# Paste your Reown project id into NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID.
# Needed on mobile so WalletConnect stays connected after refresh.

# Normal-mode personal bests (wallet-synced across phone + desktop):
# Vercel → Storage → Create KV Database (Hobby free). It injects:
#   KV_REST_API_URL
#   KV_REST_API_TOKEN
# Without KV, Normal PBs stay on that browser only.

# ---------------------------------------------------------------
# NEVER on Vercel:
#   PRIVATE_KEY, TREASURY private key, Foundry deploy keys.
#   Those belong only in contracts/.env (gitignored) on this machine.
#   VOUCHER_SIGNER_PRIVATE_KEY above is the ONE server-side key on
#   Vercel — it holds no funds, it only signs vouchers.
# ---------------------------------------------------------------

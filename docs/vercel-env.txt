# Vercel (production app)
# Copy these into the Vercel project (Production + Preview). Never add PRIVATE_KEY.

NEXT_PUBLIC_CHAIN=mainnet
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org
NEXT_PUBLIC_LOOPTERNITY_CONTRACT_ADDRESS=0x66b549F570Fa63e3109B85FD15678c175F1a02c9
NEXT_PUBLIC_VAULT_DEPLOY_BLOCK=49966576
NEXT_PUBLIC_P2E_ENTRY_FEE_USD=0.05
NEXT_PUBLIC_P2E_TREASURY_ADDRESS=0xEacA26c65bd43803CB07319D5055bB7CF8DEC00c
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Do NOT set NEXT_PUBLIC_P2E_ENTRY_FEE_ETH on Vercel.
# That field is an exact ETH amount. 0.05 means 0.05 ETH (~$90), not $0.05.
# Leave it unset. USD→ETH uses NEXT_PUBLIC_P2E_ENTRY_FEE_USD; the vault
# charges entryFeeWei (~$0.05).

# Paste your Reown project id into NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID (same as local .env.local).
# Needed on mobile so WalletConnect stays connected after refresh.

# Normal-mode personal bests (wallet-synced across phone + desktop):
# Vercel → Storage → Create KV Database (Hobby free). It injects:
#   KV_REST_API_URL
#   KV_REST_API_TOKEN
# Without KV, Normal PBs stay on that browser only.

# GitHub Actions keeper (Sunday 00:05 UTC)
# Repo → Settings → Secrets and variables → Actions:
#   KEEPER_PRIVATE_KEY = contracts/.env PRIVATE_KEY (owner/keeper). Never commit it.
#   BASE_MAINNET_RPC_URL = optional dedicated Base RPC (else public mainnet.base.org)
# Workflow: .github/workflows/weekly-p2e.yml
